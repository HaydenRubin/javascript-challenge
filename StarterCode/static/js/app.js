// from data.js
var tableData = data;

// YOUR CODE HERE!

// .push() is how to append an item to an array.

// .slice(x) returns items from x on (inclusive).
// .slice(x, y) returns items from x to y - 1. 

// .join() returns items of array into single string.
// e.g.: .join(", ") returns a, b, (comma, then space after each item.)

// .split(" ") every time there is a space, creates new string. 
// splits string into an array of substrings. 
